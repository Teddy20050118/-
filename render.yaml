services:
  - type: web
    name: ordering-assistant
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    # Render 會提供 $PORT，必須用它啟動
    startCommand: uvicorn src.back:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
